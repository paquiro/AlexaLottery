const { CancelIntent } = require('./_CancelIntent');
const { HelpIntent } = require('./_HelpIntent');
const { LaunchRequest } = require('./_LaunchRequest');
const { StopIntent } = require('./_StopIntent');
const { Unhandled } = require('./_Unhandled');
const { Lottery } = require('./_Lottery');

module.exports = {
  CancelIntent,
  HelpIntent,
  LaunchRequest,
  StopIntent,
  Unhandled,
  Lottery
};
